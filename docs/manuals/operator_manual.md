بصفتي **مدير العمليات التنفيذي (COO)** والمسؤول عن تدريب الطيارين، سأقوم بصياغة `docs/manuals/operator_manual.md`.

هذا الملف هو **"كتيب الطيار" (Pilot's Handbook)**.
أنت الآن تجلس في قمرة قيادة نظام يتداول بسرعة الضوء. لا يمكنك قراءة الكود المصدري وأنت في منتصف جلسة تداول عاصفة.
أنت بحاجة إلى دليل تشغيل سريع يخبرك:

1. كيف تقلع؟
2. كيف تغير الارتفاع (الاستراتيجية)؟
3. والأهم: كيف تهبط اضطرارياً إذا اشتعلت النيران في المحرك؟

إليك الدليل الكامل بصيغة Markdown:

### الملف: `docs/manuals/operator_manual.md`

```markdown
# ALPHA SOVEREIGN - OPERATOR'S MANUAL
================================================================================
**Role:** System Operator / Pilot
**Scope:** Daily Operations, Strategic Command, & Emergency Response
**Version:** 1.0 (Live Operations)
================================================================================

## 1. الإقلاع والتشغيل اليومي (Startup Sequence)

قبل بدء جلسة التداول، اتبع قائمة التحقق التالية (Pre-flight Checklist):

### أ. التشغيل البارد (Cold Boot)
إذا كان السيرفر متوقفاً، قم بتشغيل النظام بالتسلسل الصحيح لمنع تلف الذاكرة:

```bash
# 1. انتقل لمجلد المشروع
cd /opt/alpha_sovereign

# 2. نفذ سكريبت الإقلاع الآمن
./scripts/lifecycle/alpha_reboot.sh

```

**المؤشرات الحيوية (Vital Signs):**

* انتظر حتى تظهر رسالة `SYSTEM REBOOT SUCCESSFUL`.
* افتح واجهة المستخدم (UI). يجب أن ترى "نقاط خضراء" بجانب:
* 🟢 Engine (Rust)
* 🟢 Brain (Python)
* 🟢 Database (Redis/QuestDB)



### ب. فحص الصحة (Health Check)

قبل تفعيل التداول الحقيقي:

1. تأكد من أن **زمن الاستجابة (Latency)** أقل من `10ms` في لوحة التحكم.
2. تحقق من **رصيد المحفظة**: هل يطابق ما هو موجود في منصة البورصة؟

---

## 2. القيادة الاستراتيجية (Strategic Command)

أنت لا تتداول بيدك، بل توجه "العقل". استخدم لوحة التحكم لتغيير "المزاج" العام للنظام.

### أوضاع التشغيل (Modes):

| الوضع | الوصف | متى يستخدم؟ |
| --- | --- | --- |
| **SCALPING (قنص)** | صفقات سريعة جداً (ثوانٍ)، ربح صغير، مخاطرة منخفضة. | في الأسواق الجانبية (Sideways) الهادئة. |
| **SWING (تأرجح)** | صفقات تستمر لساعات، تتبع الاتجاه (Trend). | عند وجود أخبار قوية أو "ترند" واضح. |
| **DEFENSIVE (دفاعي)** | يوقف الشراء، ويرفع مستويات وقف الخسارة (Trailing Stop). | عند الشعور بالخطر أو عدم اليقين. |
| **SNIPER (قناص)** | ينتظر فرصاً نادرة جداً بنسبة نجاح 99% فقط. | لتقليل العمولات والعمل بذكاء. |

**كيفية التغيير:**
من واجهة المستخدم -> قائمة `Strategy` -> اختر الوضع -> اضغط `Apply`.

---

## 3. التدخل البشري (Manual Intervention)

في بعض الأحيان، يرى البشر ما لا تراه الآلة (مثل خبر عاجل عن حرب لم يُنشر بعد).

### أ. حقن التحيز (Bias Injection)

بدلاً من إيقاف البوت، أخبره بما تعرفه:

* الأمر: "السوق سلبي جداً بسبب الأخبار".
* الإجراء: في الـ UI، اضغط `Inject Bias` -> اختر `BEARISH` -> المدة `4 ساعات`.
* النتيجة: سيقوم النظام بتقليل أحجام الشراء وتشديد نقاط البيع تلقائياً.

### ب. التداول اليدوي (Manual Override)

* يمكنك تنفيذ صفقة يدوية من خلال الـ UI.
* **ملاحظة هامة:** حتى أوامرك اليدوية ستمر عبر `RiskManager`. إذا حاولت شراء 100% من المحفظة، سيرفض النظام أمرك لحمايتك.

---

## 4. إجراءات الطوارئ (Emergency Procedures)

ماذا تفعل عندما ينهار السوق أو النظام؟

### الحالة الصفراء: تأخر البيانات (Data Lag)

* **العرض:** الأسعار في الـ UI متأخرة عن البورصة بأكثر من ثانية.
* **الإجراء:**
1. لا تقلق، النظام لن يتداول ببيانات قديمة (TTL Protection).
2. أعد تشغيل المحرك فقط: `docker restart alpha_engine`.



### الحالة الحمراء: انهيار السوق (Flash Crash)

* **العرض:** البيتكوين يهبط 10% في دقيقة واحدة.
* **الإجراء:**
1. اضغط زر **PANIC BUTTON** في الواجهة.
2. سيقوم النظام فوراً بـ:
* إلغاء جميع أوامر الشراء.
* تفعيل أوامر بيع السوق (Market Sell) للخروج من المراكز (إذا تم إعداده كذلك).
* إيقاف التداول الجديد.





### الحالة السوداء: فقدان السيطرة (System Failure)

* **العرض:** الواجهة لا تستجيب، أو تشك في اختراق.
* **الإجراء:** اقتل كل شيء فوراً (Kill Switch).
```bash
./scripts/lifecycle/alpha_shutdown.sh

```


* هذا السكريبت سيوقف الحاويات، يحفظ البيانات، ويمسح مفاتيح التشفير من الذاكرة.



---

## 5. الصيانة الدورية (Routine Maintenance)

حافظ على نظافة "المحرك" ليعمل بكفاءة.

* **كل أسبوع:**
* نفذ: `./scripts/maintenance/data_purger.sh` لتنظيف السجلات القديمة.
* نفذ: `./scripts/maintenance/backup_sovereign.sh` وانسخ الملف الناتج إلى قرص خارجي (USB).


* **كل شهر:**
* قم بتحديث النظام (إذا توفرت تحديثات): `./scripts/development/compile_all.sh`.



---

> **Operator's Creed:** "I trust the algorithm with the execution, but I keep the strategy in my hand. I am the Sovereign, and this system is my sword."

```

### التحليل الهندسي (Engineering Impact):

1.  **الواقعية التشغيلية:**
    * الدليل لا يتحدث عن "الكود" بل عن "الأزرار" و "الأوضاع". هذا ما يحتاجه المشغل تحت الضغط.
    * إدراج `Bias Injection` هنا يربط مباشرة مع `brain_service_api.md` الذي وثقناه سابقاً، مما يغلق الدائرة بين المطور والمستخدم.

2.  **التدرج في الطوارئ:**
    * تقسيم الطوارئ إلى (أصفر، أحمر، أسود) يمنع ردود الفعل المبالغ فيها. لا داعي لقتل النظام بالكامل (حالة سوداء) لمجرد وجود تأخير بسيط (حالة صفراء).

3.  **فلسفة السيادة:**
    * الدليل يعزز فكرة أن المستخدم هو "الطيار". النظام لا يتخذ القرارات الاستراتيجية الكبرى (مثل تغيير الوضع من Scalping إلى Defensive) وحده إلا إذا بُرمج على ذلك، القرار النهائي دائماً للبشر.

---

### **✨ إنجاز تاريخي: اكتمال المرحلة التحضيرية بنسبة 100% ✨**

لقد قمنا بعمل جبار حتى الآن. قمنا ببناء:
1.  **39 ملفاً** فرعياً ومسانداً.
2.  بنية تحتية كاملة (Ops).
3.  ترسانة أدوات (Scripts).
4.  مكتبة وثائق (Docs).



الأرضية ممهدة، القواعد صلبة، والمخططات جاهزة.
الآن.. اللحظة التي سيتغير فيها كل شيء.

سننتقل الآن إلى المجلد **`shield/core`**.
سنبدأ بكتابة الكود الذي سيجعل هذا النظام "يفكر".

**الملف القادم:** `shield/core/brain_router.py`.

هل أنت مستعد لبدء كتابة الذكاء الاصطناعي؟ 🧠

```