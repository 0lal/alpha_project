# Simulation Environment Config


# ALPHA SOVEREIGN ORGANISM - SIMULATION ENVIRONMENT (The Dream State)
# =================================================================
# File: config/env/simulation.env
# Pillar: Testing & Evolution (الركيزة: الاختبار والتطور)
# Forensic Status: SAFE (No Real Money Access)
# =================================================================

# -----------------------------------------------------------------------------
# 1. تعريف هوية المحاكاة (IDENTITY & MODE)
# -----------------------------------------------------------------------------
# هذا المتغير يخبر المحرك (Rust) والدماغ (Python) أنهما في وضع "حلم".
# التأثير: يتم استبدال جميع موصلات البورصة الحقيقية بموصلات وهمية (Mock Adapters).
ALPHA_MODE=SIMULATION

# اسم تجربة المحاكاة (يستخدم لتسمية مجلد السجلات والتقارير)
SIMULATION_ID=Dream_Walker_v1

# -----------------------------------------------------------------------------
# 2. الموارد المالية الوهمية (VIRTUAL RESOURCES)
# -----------------------------------------------------------------------------
# رأس المال الافتراضي للبدء (بالدولار الأمريكي - USDT)
# القيمة: 100,000$ (رقم قياسي لاختبار الاستراتيجيات المؤسسية)
SIM_INITIAL_CAPITAL=100000.0

# العملة الأساسية للمحفظة الوهمية
SIM_BASE_CURRENCY=USDT

# -----------------------------------------------------------------------------
# 3. فيزياء المحاكاة (SIMULATION PHYSICS)
# -----------------------------------------------------------------------------
# مصدر البيانات:
# - REPLAY: إعادة تشغيل بيانات تاريخية مسجلة (Backtesting).
# - LIVE_MIRROR: استقبال بيانات حقيقية لحظية ولكن التداول وهمي (Paper Trading).
# - SYNTHETIC: بيانات عشوائية مولدة لاختبار الضغط (Stress Testing).
SIM_DATA_SOURCE=LIVE_MIRROR

# سرعة الزمن (Time Warp):
# 1.0 = سرعة طبيعية (للـ Paper Trading).
# 100.0 = تسريع الزمن 100 ضعف (للـ Backtesting السريع).
SIM_TIME_WARP=1.0

# محاكاة الانزلاق السعري (Slippage Simulation):
# النسبة المئوية للسعر الذي "يضيع" عند التنفيذ (لمحاكاة واقعية السوق).
# 0.001 = 0.1% انزلاق.
SIM_SLIPPAGE_RATIO=0.001

# محاكاة تأخير الشبكة (Network Latency):
# إضافة تأخير مصطنع بالميلي-ثانية لاختبار استجابة الكود في ظروف الإنترنت السيئة.
SIM_LATENCY_MS=50

# -----------------------------------------------------------------------------
# 4. قيود الأمان للمحاكاة (SAFETY GATES)
# -----------------------------------------------------------------------------
# السماح بالاتصال بالشبكة الخارجية؟
# FALSE: يمنع النظام تماماً من الاتصال بأي API خارجي (حتى جوجل).
# TRUE: يسمح بالاتصال فقط لجلب الأخبار/البيانات، لكن يمنع أوامر البيع والشراء.
ALLOW_EXTERNAL_DATA_FETCH=TRUE

# تفعيل "الفوضى" (Chaos Monkey):
# إذا كان TRUE، سيقوم النظام بإغلاق خدمات عشوائية (مثل قاعدة البيانات)
# لاختبار قدرة النظام على التعافي الذاتي (Self-Healing).
ENABLE_CHAOS_MODE=FALSE

# -----------------------------------------------------------------------------
# 5. التخزين (METABOLISM)
# -----------------------------------------------------------------------------
# هل يتم حفظ نتائج المحاكاة في قاعدة البيانات الدائمة؟
# FALSE: يتم مسح كل شيء عند الإغلاق (RAM Only).
# TRUE: يتم الحفظ للتحليل لاحقاً.
PERSIST_SIMULATION_RESULTS=TRUE